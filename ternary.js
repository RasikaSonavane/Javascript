var age=20
var cardrive = age>18 ? 'Yes' : 'No';
console.log(cardrive)

//instead of if else we can use ternary operator to make our code more efficient clean 
// easy to understand

var nationality= "indian"
var result = nationality=="indian" ? 'fine' : 'you are not eligible';
console.log(result)